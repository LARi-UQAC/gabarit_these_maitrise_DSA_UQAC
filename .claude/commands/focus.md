# Focus on a specific topic

Scope this session to: $ARGUMENTS

Rules:

- Ignore files, sections, and prior context not directly related to $ARGUMENTS
- If something in the context is irrelevant, do not mention it
- If you need a specific file, ask explicitly rather than inferring from memory

Read only the necessary files, then address $ARGUMENTS directly.
Respond in French unless the active file or codebase is in English.
